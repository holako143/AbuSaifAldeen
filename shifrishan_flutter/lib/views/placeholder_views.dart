// Placeholder views for basic structure
import 'package:flutter/material.dart';

class SteganographyView extends StatelessWidget {
  const SteganographyView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("تبويب تشفير الصور - قيد التنفيذ"));
  }
}

class VaultView extends StatelessWidget {
  const VaultView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("الخزنة السرية - قيد التنفيذ"));
  }
}
